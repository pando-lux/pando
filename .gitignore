node_modules/
dist/
*.tsbuildinfo
.env*
.DS_Store
*.log
package-lock.json

# QA screenshots (evidence stored in QA reports, not committed separately)
*.png
qa-phase*/
nul

# Temporary files
tmp-*.json

# @pando/tests local results (per-project, not committed)
.pando-tests/
.pando-code.db
.pando-code.db-shm
.pando-code.db-wal

# Worker context files (runtime artifact, not source)
.pando-worker-context.md

# Launch scripts with embedded secrets
start-with-keys.sh

# Deploy backups and temp files
.deploy-backups/
claude-manager/
stderr.tmp

# Private dev testing guide (contains AWS credentials)
dev-testing/
.vercel

# E2E test results (runtime artifacts)
e2e/results/

# QA E2E screenshots (generated, not source)
qa-e2e/

# Accidental path-mangled files
Usersjaira*

# Old planning docs
*-PLAN.md
*-RESULTS.md
RESULT.md

# QA snapshot/state files (runtime artifacts)
*snapshot*.md
project-state.md
